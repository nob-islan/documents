# メール送信設定

メール送信設定をします。例として、同じdocker-compose内の`mailpit`に送信する設定を入れます。

cf. https://docs.gitlab.com/omnibus/settings/smtp/

## 設定

```yaml
gitlab_rails['smtp_enable'] = true
gitlab_rails['smtp_address'] = 'mailpit'
gitlab_rails['smtp_port'] = 1025
```
