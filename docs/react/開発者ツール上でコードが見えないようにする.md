# 開発者ツール上でコードが見えないようにする

ビルド後に実行したReactアプリについて開発者ツール上でコードを読めなくするには、`package.json`について、下記のように変更します:

```diff
   "scripts": {
-    "build": "react-scripts build",
+    "build": "GENERATE_SOURCEMAP=false react-scripts build",
   },
```
